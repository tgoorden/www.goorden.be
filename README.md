INSTALLATION NOTES:

- requires dart-sass to be present on the command line ("which sass" should point to /usr/local/bin/sass)


TODO:

Script + layout:
- alt images
- short previews

Layout
- Add external links
